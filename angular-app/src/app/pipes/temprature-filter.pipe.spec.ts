

import { TempratureFilterPipe } from './temprature-filter.pipe';

describe('TempratureFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TempratureFilterPipe();
    expect(pipe).toBeTruthy();
  });
});


