

import { FontSizeDirective } from './font-size.directive';

describe('FontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeDirective(null);
    expect(directive).toBeTruthy();
  });
});


